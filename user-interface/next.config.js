/** @type {import('next').NextConfig} */

const nextConfig = {
    darkMode: 'class',
    reactStrictMode: true,
    output: "export",
    images: {
      unoptimized: true
    }
}

module.exports = nextConfig